node_modules/
.svelte-kit/
build/
dist/
src/lib/types/
src-tauri/target/
*.db

# Hand-authored HTML containing mermaid blocks with `<id>` placeholders
# that prettier's HTML parser misreads as unclosed tags.
docs/flow-security-walkthrough.html

# Markdown is hand-authored. Prettier's table realignment and emphasis
# escaping are high-churn, low-value for prose — and non-idempotent on some
# files (e.g. italic spans containing `_`/`*`), which made format:check
# unfixable. CHANGELOG.md is generated by the deploy script.
*.md
