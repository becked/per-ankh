node_modules/
.svelte-kit/
build/
dist/
src/lib/types/
src-tauri/target/
*.db

# docs/ is hand-authored — design notes plus HTML walkthroughs whose mermaid
# `<id>` placeholders prettier's HTML parser misreads as unclosed tags. Keep
# prettier (incl. its Tailwind class sorter) out of it entirely.
docs/

# Markdown is hand-authored. Prettier's table realignment and emphasis
# escaping are high-churn, low-value for prose — and non-idempotent on some
# files (e.g. italic spans containing `_`/`*`), which made format:check
# unfixable. CHANGELOG.md is generated by the deploy script.
*.md
