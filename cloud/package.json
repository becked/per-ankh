{
	"name": "per-ankh-share-api",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"d1:init": "wrangler d1 execute per-ankh-share-index --file=schema.sql",
		"test": "vitest"
	},
	"dependencies": {
		"nanoid": "^5.0.0"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.5.0",
		"@cloudflare/workers-types": "^4.20241205.0",
		"typescript": "^5.6.0",
		"vitest": "^2.1.0",
		"wrangler": "^3.91.0"
	}
}
