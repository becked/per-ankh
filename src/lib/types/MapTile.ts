// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ReligionInfo } from "./ReligionInfo";

/**
 * Tile data for map visualization
 */
export type MapTile = { x: number, y: number, terrain: string | null, height: string | null, vegetation: string | null, resource: string | null, improvement: string | null, improvement_pillaged: boolean, has_road: boolean, specialist: string | null, tribe_site: string | null, 
/**
 * All religions present in this tile's city (up to 5)
 */
religions: Array<ReligionInfo>, river_w: boolean, river_sw: boolean, river_se: boolean, 
/**
 * Resolved from owner_player_id -> players.nation
 */
owner_nation: string | null,
/**
 * xml_id of the player owning this tile at the represented turn (the
 * companion to owner_nation, which loses the player in same-nation mirror
 * matches). Drives per-turn resolution of a city's ruling family on the map.
 * Optional: absent on blobs parsed before PARSER_VERSION 2.10.0.
 */
owner_player_xml_id?: number | null,
/**
 * Resolved from owner_city_id -> cities.city_name
 */
owner_city: string | null,
/**
 * True if this tile is a city center
 */
is_city_center: boolean, 
/**
 * True if this tile is a capital city center
 */
is_capital: boolean, };
